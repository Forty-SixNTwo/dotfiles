- defaults:
  links:
    relink: true

- clean: ['~']

- link:
  ~/.zshrc:
    path: ./source/zshrc
  ~/.gitconfig:
    path: ./source/gitconfig

- create: {}

- shell:
  - command: ./bin/homebrew/install.sh
    stdout: true
    stderr: true
    when: "not os.path.exists('/usr/local/bin/brew')"